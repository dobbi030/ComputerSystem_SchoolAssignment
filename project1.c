#include <stdio.h>

unsigned replace_byte (unsigned x, int i, unsigned char b)
{
//    (&x) + i
}

int main(void)
{
    unsigned x = 0x12345678;
    
    printf(&x + 1);
    printf(&x + 2);
    
    return 0;
}
